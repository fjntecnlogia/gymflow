[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma@5 generate", "npm run build"]

[start]
cmd = "npx prisma@5 db push --accept-data-loss && node dist/server.js"
